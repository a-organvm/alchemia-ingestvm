# DISCOVERY - organvm/alchemia-ingestvm

**Verdict:** VALUE FOUND -> promote into the ranked tier.
**Date:** 2026-07-01 (auto-discovery)

## Value Thesis

`organvm/alchemia-ingestvm` is the estate's source-material refinery: a working Python CLI and library that turns a scattered workspace, notes, bookmarks, Google Docs, AI chat transcripts, screenshots, and manifest metadata into fingerprinted inventory, organ/repo classifications, deployment manifests, provenance records, and reusable aesthetic prompt briefs. Its highest latent value is not as a standalone SaaS yet; it is as the reusable "context ingestion and provenance" layer that makes the rest of ORGANVM build faster by converting dark corpus material into repo-ready, traceable work packages while preserving style DNA through `taste.yaml` and per-organ creative briefs. That capability also has an honest revenue path as a packaged knowledge-migration and AI-context-operations service for creators, agencies, and small teams with years of unstructured chats/docs/bookmarks that need to become searchable, versioned, agent-usable project memory. The code is already beyond archival: it exposes real commands (`intake`, `absorb`, `alchemize`, `capture`, `sync`, `synthesize`, `gdocs-auth`, `gdocs-status`), has focused tests across crawler/classifier/channels/provenance/deployment planning, and declares production responsibilities for classified artifacts, deployment manifests, provenance records, creative briefs, and aesthetic chains. Its single best concrete first task is to add a read-only `alchemia report` command that runs or consumes intake/absorb data and emits a Markdown plus JSON discovery report ranking target repos, deployable/reference/skipped files, pending-review items, duplicate clusters, and provenance coverage without calling `gh`; this would convert the existing pipeline into an inspectable value-discovery surface that can feed human review, downstream build tickets, and later gated deployment.
